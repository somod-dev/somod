{
  "name": "@sodaru-cli/slp",
  "version": "1.4.0",
  "description": "ServerLess Package Builder",
  "keywords": [
    "sodaru",
    "slp",
    "package",
    "builder"
  ],
  "author": "Raghavendra K R <raghavendra@sodaru.com>",
  "homepage": "https://gitlab.com/sodaru/cli/package-managers/packages/slp#readme",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "bin": {
    "njp": "./bin/slp.js"
  },
  "files": [
    "bin",
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+ssh://git@gitlab.com/sodaru/cli/package-managers.git"
  },
  "scripts": {
    "clean": "npx rimraf dist",
    "bundle": "npx esbuild src/index.ts --bundle --outdir=dist --platform=node --minify --external:@sodaru-cli/serverless-schema",
    "build": "npm run clean && npm run bundle",
    "pretest": "$CI || npm run build",
    "test": "echo 'No tests defined'",
    "prepack": "npm run test"
  },
  "peerDependencies": {
    "typescript": "^4.3.5"
  },
  "todo": "TODO: add @sodaru-cli/serverless-schema as peer dependency after publishing the fiest version in to npm registry",
  "devDependencies": {
    "@sodaru-cli/base": "^1.2.0",
    "@sodaru-cli/package-manager-lib": "^1.4.0",
    "@sodaru-cli/serverless-schema": "^1.4.0",
    "@types/node": "^16.4.13",
    "esbuild": "^0.12.28",
    "typescript": "^4.3.5"
  }
}
