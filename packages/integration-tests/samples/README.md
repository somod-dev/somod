# Sample SOMOD Modules

## Install

The sample modules have dependencies on `somod` and are also interdependent

> So It is advised to start the npm proxy before running `npm i` inside these modules for development purposes.
