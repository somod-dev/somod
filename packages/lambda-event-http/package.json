{
  "name": "@somod/lambda-event-http",
  "version": "1.4.4",
  "description": "Http handler for lambda functions",
  "main": "./dist/cjs/index.js",
  "module": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@sodev/jest-config": "^1.1.2",
    "@solib/errors": "^1.1.1",
    "@solib/json-validator": "^1.0.1",
    "@types/json-schema": "^7.0.9",
    "@types/aws-lambda": "^8.10.92",
    "rimraf": "^3.0.2",
    "tslib": "^2.3.1",
    "typescript": "^4.5.5"
  },
  "peerDependencies": {
    "@solib/errors": "^1.1.1",
    "@solib/json-validator": "^1.0.1",
    "tslib": "^2.3.1"
  },
  "scripts": {
    "clean": "npx rimraf dist",
    "tsc-cjs": "npx tsc --project tsconfig.cjs.json",
    "tsc": "npx tsc",
    "build": "npm run clean && npm run tsc && npm run tsc-cjs",
    "pretest": "npm run build",
    "test": "npx jest",
    "prepack": "npm run test",
    "postversion": "git push --follow-tags"
  }
}
